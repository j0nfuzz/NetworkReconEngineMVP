# Publication-safe identity example. Historical source values are deliberately absent.
# This file does not rewrite or anonymise existing commit objects.
PERSON <person@example.com> <person@example.com>
