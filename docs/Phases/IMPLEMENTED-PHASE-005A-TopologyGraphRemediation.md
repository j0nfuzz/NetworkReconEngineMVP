PHASE: PHASE-005A-TopologyGraphRemediation

STATUS: Implemented

FILES MODIFIED:
- app/topology.py
  - build_topology_graph() now materializes the incoming `summaries` iterable to a list with `summaries = list(summaries)` before iterating twice for nodes and edges.
  - Removed the now-unused `collected_names` set.
- tests/test_cli.py
  - Added `test_build_topology_graph_from_generator_produces_edges()` to prove generator inputs still yield edges.

TESTS ADDED:
- tests/test_cli.py::test_build_topology_graph_from_generator_produces_edges

DDR UPDATES:
- UNCHANGED DD:2026-08-04

RISKS INTRODUCED:
- None. Materializing a generator to a list is bounded by the number of collected devices and matches the existing list-based test expectations.

RISKS RESOLVED:
- Topology graph generated by the CLI no longer loses neighbor/edge data because the input generator was exhausted during the first pass.

OPEN ISSUES:
- CDP/LLDP neighbor names may not match configured device names, producing orphaned edges (carried forward from PHASE-004/005).
